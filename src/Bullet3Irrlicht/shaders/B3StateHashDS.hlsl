// Emulated-double (df64) wrapper; the kernels live in the shared body.
// PREFER_FLOW_CONTROL - the marker is only read from the top-level source, never an .hlsli.

#define OS_DS 1
#include "B3StateHashBody.hlsli"
