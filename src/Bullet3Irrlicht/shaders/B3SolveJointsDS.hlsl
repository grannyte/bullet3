// PREFER_FLOW_CONTROL - why, see B3SolveJoints.hlsl; the marker must sit in each top-level file.
// Emulated-double (df64) wrapper; the kernels live in the shared body.

#define OS_DS 1
#include "B3SolveJointsBody.hlsli"
